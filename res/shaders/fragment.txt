#version 460
in vec2 texCoords;
out vec4 texColor;

uniform sampler2D tex;

void main()
{
	texColor = texture(tex, texCoords);
	vec4 pre_texColor = texColor;
	if (pre_texColor == vec4(1.0, 1.0, 1.0, 1.0))
	{
		discard;
	}
}